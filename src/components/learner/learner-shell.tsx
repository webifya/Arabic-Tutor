import Link from "next/link";
import type { Messages } from "@/i18n/types";
import { LogoutButton } from "@/components/auth/logout-button";

const activeItems=[{href:"/learn",key:"home",icon:"⌂"},{href:"/learn/course/arabic-foundation-bn",key:"learn",icon:"◉"},{href:"/profile",key:"profile",icon:"○"},{href:"/settings",key:"settings",icon:"⚙"}] as const;
const futureItems=[{key:"review",icon:"↻"},{key:"practice",icon:"✦"},{key:"tutor",icon:"✧"}] as const;
export function LearnerShell({children,messages,mode,name}:{children:React.ReactNode;messages:Messages["learner"];mode:"standard"|"child";name:string}){return <div className={`learner-app mode-${mode}`}><aside className="learner-sidebar"><Link className="learner-logo" href="/learn"><span lang="ar" dir="rtl">لسان</span><small>Lisan</small></Link><nav aria-label="Primary">{activeItems.map(item=><Link key={item.key} href={item.href}><span aria-hidden>{item.icon}</span>{messages.nav[item.key]}</Link>)}{futureItems.map(item=><span className="nav-disabled" key={item.key} title={messages.nav.soon}><span aria-hidden>{item.icon}</span>{messages.nav[item.key]}<small>{messages.nav.soon}</small></span>)}</nav><div className="sidebar-user"><div className="avatar-placeholder">{name.trim().charAt(0).toUpperCase()}</div><span>{name}</span><LogoutButton/></div></aside><div className="learner-main">{children}</div><nav className="mobile-nav" aria-label="Mobile primary">{activeItems.slice(0,3).map(item=><Link key={item.key} href={item.href}><span aria-hidden>{item.icon}</span><small>{messages.nav[item.key]}</small></Link>)}</nav></div>}
